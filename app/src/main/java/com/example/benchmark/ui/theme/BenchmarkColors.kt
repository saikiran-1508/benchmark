package com.example.benchmark.ui.theme

import androidx.compose.ui.graphics.Color

// --- Dark Mode / Monochrome Palette ---

// Backgrounds
val BgColor = Color(0xFF000000)       // Pure Black Background
val CardColor = Color(0xFF121212)     // Very Dark Gray for cards
val SurfaceColor = Color(0xFF333333)  // Dark Gray for placeholders

// Text
val PrimaryText = Color(0xFFFFFFFF)   // Pure White for main text
val SecondaryText = Color(0xFFB0B0B0) // Light Gray for secondary info

// Elements & Accents
val DividerColor = Color(0xFF333333)  // Dark Gray for lines
val DarkAccent = Color(0xFFFFFFFF)    // White for active elements (High Contrast)
val LightAccent = Color(0xFF666666)   // Dim Gray for completed items

// Buttons
val ButtonColor = Color(0xFFFFFFFF)   // White Button (stands out on black)
val OnButtonColor = Color(0xFF000000) // Black Text on the White Button